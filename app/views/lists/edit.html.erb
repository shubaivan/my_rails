<% provide(:title, 'Edit list') %>
<% provide(:button_text, 'Save changes') %>
<h1>Update list</h1>

shared user lists:

<% @list.lists_user.each do |lists_user| %>
    <%= lists_user.user.email %>
<% end %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>