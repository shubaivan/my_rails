<% provide(:title, @user.name) %>

<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
        <br>

        Lists:
        <% @user.lists.all.each do |list| %>
            <%= link_to list.title, edit_list_path(list) %><br>
        <% end %>

      </h1>
    </section>
  </aside>
</div>